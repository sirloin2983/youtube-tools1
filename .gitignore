# 実行時に生成されるファイル(コードではないので追跡しない)
__pycache__/
*.pyc
*.log
*.crash.log
*.bak

# 旧ツール一式(開発対象外。APIキー・モデル本体・個人データを含むため丸ごと除外)
0old/

# 実行のたびに変わる状態・キャッシュ・個人データ・秘密情報(全ツール共通)
**/cache/
**/archive/
**/work/
**/exports/
**/clips/
**/dataset/
**/transcripts/
**/models/
.whisper_models/
**/config.json
**/settings.json
**/settings-ui.json
**/registry.json
**/data.json
**/data.json.bak
**/data.json.corrupt-*
**/feedback.jsonl
**/feedback.jsonl.old
**/.running.json
**/*.onnx

# cut2resolve / srt2resolve の変換結果フォルダ
*_edl/
*_pack/
*_resolve/

# 動画・音声・圧縮ファイル(コードではない)
*.mp4
*.mov
*.mkv
*.webm
*.wav
*.json.gz
*.zip

# 受け渡し用のファイル(パッチ・Claude の出力フォルダ)
git-patches/
*.patch
Claude outputs/
_recovered/

# OS
Thumbs.db
.DS_Store

# 実行中のサーバーのポート(docs/pipeline.md の 4)
.runtime/
# Claude(Cowork)が納品する確認用フォルダ
_new/
# 古い名前のログの控え・壊れた登録ファイルの退避(clip-studio)
*.log.old
**/registry.json.corrupt-*
# 入口の記録(画面のエラーの記録 client-errors.jsonl など)・窓(Edge のアプリモード)の専用のプロファイル(段階7。ふだんは作業データの置き場所。YTT_DATA_DIR=inplace のときだけ app の下)
**/logs/
**/browser-profile/

