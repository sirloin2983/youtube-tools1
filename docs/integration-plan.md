# ツールの統合計画(2026-09-24 作成・2026-09-26 更新)

3つのツール(切り抜きスタジオ・文字起こしツール・cut2resolve)を、1つのアプリとして使えるようにするための計画。
本文(議論・比較の全文)は claude.ai の Claude Docs「動画編集ツール 統合計画」(**正本**)。ここは実装する AI が読むための要約。
食い違ったら Claude Docs が正。claude.ai の Project の `claude/*.md` と `docs/project/` は写しで、古いことがある。

## 決まっていること
- **作り直しはしない**。今の構成(Python 標準ライブラリ中心のサーバー + 画面)のまま、新しい入れ物が既存のツールを1つずつ取り込む
- **段階3 の統合対象は、スタジオ・文字起こし・cut2resolve の3つ**(ユーザー判断 2026-09-25。09-24 の「cut2resolve 以外を先に」から拡大)。
  理由: 09-25 に cut2resolve v0.4.0 の Text+ パックが Resolve 実機で成功した(60fps 横の動画 → 30fps のタイムラインで字幕の位置一致を確認)。
  以前の除外理由(Resolve 実機未確認・形が決まっていない)は解消した。**友人の PC での実機確認も済んだ**(09-25 ユーザー確認)
- Resolve への渡し方は **Text+ パックが本命**。EDL は Text+ の予備として残し、EDL 取り込みの実機確認は当面しない(ユーザー判断 09-25)
- **ゲート条件(段階3 に入る前に必ず)**: cut2resolve の未コミットの変更(0.3.2 → 0.4.0、11ファイル)にテストを足して実行し、コミットする。
  対象: `resolve_textplus.py`・`pack.py`・`cut2resolve.py`・`serve.py`・`cut2resolve_core.py`・`app.js`・`index.html`・`README.txt`・`test_pack.py`・`test_serve.py`・`resolve_lua_mock.lua`(新規・未追跡)。
  `git status` で cut2resolve/ に未コミットが無いことを確かめてから段階3 を始める。
  テストは 09-25 に済(test_pack 53・test_cut2resolve 119(Text+ の CLI 経路 2件を追加)・test_serve 23・e2e_ui・ytt_core・app・tools/e2e_pipeline すべて OK)。**残りはコミットだけ**
- 取り込みの順(決定 09-25): **スタジオ → cut2resolve → 文字起こし**。
  cut2resolve は小さく(serve.py 約980行)、重い処理は ffmpeg の別プロセスなので1プロセスに入れても安全で、担当も Claude。
  文字起こしは最大規模(serve.py 約3,800行)で、認識を別プロセスのワーカーに分ける作業が要り、精度改善も並行して進んでいるので最後
- 取り込み方式(決定 09-25): **ファイルを動かさずに取り込む**(`git mv` によるパッケージ化はしない)。
  統合サーバーが各ツールの `serve.py` を別の名前(例 `studio_serve`)で読み込み、ツールのフォルダを `sys.path` に足して、ツールの中の import(`import common` など)はそのまま使う。
  理由: Claude(Cowork)は PC でファイルの移動・改名・git を実行できない。移動しなければ旧起動方法(各ツールの start.bat・`serve.py`)もそのまま動き、git mv による他の AI との競合も起きない。
  守ること: ツール間で**同じ名前の Python モジュールを作らない**(09-25 時点で重なるのは `serve.py` と画面テストの `e2e_ui.py` だけ)。これをテストで検査する。
  各ツールの `serve.py` は、import しただけでサーバーやスレッドが動かない形にする(起動は `main()` の中だけ)
- cut2resolve を取り込むまでの間は、今のまま別アプリ(:8810)で、URL のリンク + ファイル(transcript/v1・cut-plan/v1・SRT)で連携する。`.runtime/` と `/api/siblings` もそれまで残す
- Resolve パックの二重実装(文字起こしの `resolve_export.py` と cut2resolve の `pack.py`)は段階3 で一本化する。**先に契約テスト**(同じ入力から同じパックの中身ができること)を書いてから寄せる(下の「リスク」)。
  → **09-26 実施**: 契約テスト `tools/test_resolve_pack_contract.py` を緑にしてから、文字起こしの zip も `pack.py` の Text+ パックで作るようにした(文字起こし v0.12.0・cut2resolve v0.6.0)。
  経緯と旧との違いは `docs/resolve-pack-unification.md`
- **個別ツールの完成の基準**(決定 09-25): 主要機能が動けば、段階3 に入り、統合しながら並行で仕上げる(完成を待たない)
- **統合作業の担当**(決定 09-25): Claude が主担当。担当中のツール・ファイルは、他の AI が触らない(`AGENTS.md` の「担当表」)
- **作業データの置き場**(決定 09-25): リポジトリの外(PC 上の別フォルダ)。段階4 の案件データ・移行先もここ。具体的な場所は段階4 の着手時に決める → **09-26 決定: `%LOCALAPPDATA%\youtube-tools\<ツールID>\`・全部を移す・コピーで移行し元は残す**(`docs/data-location.md`)
- **旧ツールの起動方法**(決定 09-25): 統合後も残す(移行期間の保険)。各ツールの start.bat・`serve.py [ポート]` と入口(`start-all.bat`)は消さない
- **画面の表示**(ブラウザのまま / pywebview): 段階3 の実装後に検討(保留)。それまではブラウザのまま
- 段階0〜2 はツールの開発と並行してよい

## 案の比較(要点)
| 案 | 中身 | 判断 |
|---|---|---|
| A. 起動役 + 入口の画面 | 3つのサーバーをまとめて起動・終了、入口から各ツールへ | 段階1として実装済み(`app/`) |
| B. 中継サーバー(1ポート) | 1つのポートで受けて各サーバーへ流す | 見送り(動画の Range・長い通信の中継が不具合の種。C で捨てるコード) |
| C. 1つのサーバーに取り込む | 各ツールをパッケージ化し、統合サーバーが `/studio` `/transcribe` `/cut` を受け持つ。重い処理は別プロセス | 本命(段階3) |
| D. 作り直し(FastAPI・React・Electron など) | 一から | 不採用(約100件の修正とテストを捨てる・依存が増える) |

## 段階
| 段階 | 中身 | 状態 |
|---|---|---|
| 0. 準備 | cut2resolve v0.4.0 のテスト・コミット(**段階3 のゲート**)、実機確認(`docs/review/README.md`)、判断待ち6件、この文書、担当表 | ゲートは通過(0.4.0 は 09-25 18:01 にコミット済み。Text+ は友人の PC でも確認済み) |
| 1. 入口の一本化 | `start-all.bat` 1本で3つのサーバーを子プロセスとして起動・終了。入口の画面(http://localhost:8700/)に状態・開く・起動・停止・再起動・ログ・すべて終了。ツールのコードは触らない | **実装済み(入口 v0.1.0)**。Windows 実機で確認済み(09-24) |
| 2. 共通コア `ytt_core` | 受け渡しの形式(clip/v1)・安全検査(Host/Origin/Sec-Fetch)・原子的な書き込み・`.runtime` とポートの問い合わせ・外部プログラムの場所を切り出す(スタジオ・文字起こし・入口) | **実装済み(09-24)**。ffmpeg の呼び出しとジョブの型は段階3へ(下の「段階2で決めたこと」) |
| 3. 1つのサーバーへ | スタジオ → cut2resolve → 文字起こし の順に、ファイルを動かさずに1つずつ取り込む(入口のサーバーが serve.py を別名で読み込み、`/studio/` などで受け持つ)。文字起こしの認識(faster-whisper)は別プロセスのワーカーへ。Resolve パックを一本化(契約テストが先) | **3-1 スタジオ: 実装済み(09-25。入口 v0.2.0・スタジオ v0.3.0)。3-2 cut2resolve: 実装済み(09-25。入口 v0.3.0・cut2resolve v0.5.0)。3-3 文字起こし: 実装済み(09-25。入口 v0.4.0・文字起こし v0.11.0。認識は別プロセスのワーカー)。Windows 実機で確認済み(09-26・本物の faster-whisper)。Resolve パックの一本化: 実装済み(09-26。文字起こし v0.12.0・cut2resolve v0.6.0。実機確認待ち)**。段階3は完了 |
| 4. 案件とジョブ管理 | 配信1本 = 案件。切り抜き・文字起こしを紐づける。データはコピーして移行(元は残す)。同時実行の上限。文字起こし → スタジオへ返す | **作業データの置き場所の移動: 実装済み・実機確認済み(09-26。スタジオ v0.4.0・文字起こし v0.13.0・cut2resolve v0.7.0・入口 v0.5.0)。案件ファイル・同時実行の上限・スタジオへ返す: 実装済み(09-26。入口 v0.6.0・スタジオ v0.5.0・文字起こし v0.14.0・cut2resolve v0.8.0。実機確認待ち)**。下の「段階4で決めたこと」 |
| 5. 一括実行と新機能 | まとめて実行、新着配信の監視、ラウドネス調整 | **まとめて実行・ラウドネス調整: 実装済み・実機確認済み(09-26。入口 v0.7.0・スタジオ v0.6.0・cut2resolve v0.9.0)**。新着配信の監視は保留(09-26 ユーザー決定) |
| 6. 仕上げ | README の一本化。旧ツールの起動方法は統合後も残す(09-25 決定。いつ廃止するかは統合版が実機で安定してから決める) | **README の一本化(リポジトリ直下の README.txt)・各ツールの start.bat の廃止(09-26 ユーザー決定「もうやめていい」): 実装済み**。コミット前の個人データ検査も push.bat に足した |
| 7. 画面の形(09-26 追加) | 7-0 画面のエラーの記録・7-1 解析の設定をサーバーへ・7-2 離れた/戻ったの共通化・7-3 Edge のアプリモードの窓(試用。既定はブラウザ)。7-4 pywebview は試用のあとで判断 | **7-0〜7-3: 実装済み(09-26。入口 v0.8.0・スタジオ v0.7.0・文字起こし v0.14.2・cut2resolve v0.9.1。実機確認待ち)**。下の「段階7で決めたこと」 |

## 段階1(入口)の約束 — 各ツールを変えるときに守ること
入口(`app/launch.py`)は各ツールの次の約束に依存している。変えるときは `app/launch.py` と `app/test_launch.py` も直す。
- 起動: 各ツールのフォルダで `serve.py <開始ポート> --no-open`(使用中なら次の番号。同じツールが動いていれば「すでに起動しています」で 0 終了)
- 準備完了: `.runtime/<ツールID>.json`(`{"tool", "port", ...}`)を書き、`GET /api/ping` が `{"app": ...}` を返す(`docs/pipeline.md` の 4)
- 停止: SIGTERM(Windows は Ctrl+Break = SIGBREAK)で `.runtime` を消して終わる。入口は8秒待って終わらなければ強制終了する
- 版: スタジオ・文字起こしは `serve.py` の `SERVER_VERSION = "..."`、cut2resolve は `cut2resolve_core.py` の `VERSION = "..."` を読む(別の画面で古い版が動いているときの表示用)
- 入口のポートは 8700〜8719。ツールの範囲(文字起こし 8775〜8794・スタジオ 8800〜・cut2resolve 8810〜)と重ねない

## 段階2で決めたこと(ytt_core)
- 置き場所はリポジトリ直下の `ytt_core/`(標準ライブラリだけ)。各ツールは起動時に「環境変数 `YTT_CORE_DIR` → 自分のフォルダの1つ上」の順に探して
  `sys.path` の**末尾**に足す(ツールの同名モジュールを隠さないため)。見つからなければ理由を出して起動しない
- ツールを一時フォルダに写して動かすテスト(文字起こしの e2e・test_backend・test_metrics、tools/e2e_pipeline.py・baseline_analysis.py)は
  `YTT_CORE_DIR` をリポジトリ直下にする1行を足した
- 既存の関数名(スタジオ `common.atomic_write`・`handoff.*`、文字起こし `atomic_write`・`pipeline_io.*`)は残し、中身だけ ytt_core を呼ぶ(呼び出し側とテストを変えないため)
- 動きの違いを1つにそろえたもの: 置き換えのやり直しは「Windows の winerror 5・32・33 のときだけ、4回(待ち合計 0.7 秒)」に統一
  (文字起こしは以前「Windows の PermissionError なら6回」。待ちの合計はほぼ同じ)。`.runtime` のポートは 1024〜65535 に統一(文字起こしは以前 1〜)
- fsync: 文字起こしは「失敗したら保存も失敗」(校正の成果を守る)、スタジオは「できなくても続ける」をそのまま残した(`fsync_required`)
- 段階2に入れなかったもの: ffmpeg / ffprobe の呼び出し(スタジオは ffprobe の JSON、文字起こしは ffmpeg -i の出力を読むなど、やり方が違う)と
  ジョブの型(待機列・取り消し・進み具合の持ち方がツールごとに違う)。段階3で1つのサーバーにするときに、ジョブ管理と一緒に作る
- 段階2 の時点では cut2resolve は ytt_core を使わない(当時は統合の対象外だったため)。ツールID と app の対応がずれていないことだけ `ytt_core/test_ytt_core.py` が確かめる。
  09-25 に段階3 の対象へ入ったので、ytt_core への切り替えは段階3 で取り込むときに行う

## 段階3-1(スタジオの取り込み)で決めたこと — 次の取り込みでも同じ形にする
- 置き場所: 入口と同じアドレスの `/studio/`(ユーザー判断 09-25)。入口のサーバー(`app/launch.py`)が要求の最初の行を覗いて、`/studio` で始まれば
  スタジオの Handler(`app/mount.py` が包んだもの)に、それ以外は入口の Handler に渡す。1つの接続に1つの要求(HTTP/1.0 のまま)
- 読み込み: `app/mount.py` がスタジオの serve.py を `ytt_tool_studio` という名前で読み込み、`prepare(port, "/studio/")` を呼ぶ(待ち受け以外の起動の準備。
  スタジオの main() も同じ関数を使う)。終了時は `finish()`(.runtime を消す)。取り込めなければ自動で別のプログラムとして起動する。`--no-mount` で以前の動き
- ツールの画面は**相対パス**で部品を読み、API・メディアの URL は1か所の関数で作る(スタジオは `Studio.base` を画面の場所から決める)。絶対パス `/xxx` を書かない
- `.runtime/<ID>.json` に `"path": "/studio/"` を書く。`/api/siblings` は `"paths": {"studio": "/studio/"}` を付け(無ければ付けない)、ui-kit の `UIKit.tools.setPaths()` がリンクに使う。
  問い合わせは `<path>api/ping`(同じポートでも場所が違えば別のツール)。cut2resolve の siblings も同じ規則に合わせた
- 同じアドレスになる分の安全対策: 取り込んだ画面に CSP(`script-src 'self'` + YouTube のプレイヤーだけ。インラインのスクリプト・`onclick=` は動かない)、
  書き込み系(GET・HEAD 以外)の要求に合言葉(CSRF トークン。起動ごとに作り、画面に `<meta name="ytt-token">` で渡す。ヘッダー `X-YTT-Token`)。入口の API も同じ合言葉。
  Host・Origin・Sec-Fetch の検査はツールの Handler がそのまま行う(許可するホストは入口のポート)
- 二重起動の防止: 入口の中で動いている間に start.bat でスタジオを起動すると、2つ目は立てずに入口の中のスタジオを開く(同じ data.json を取り合わないため)
- ツール間で同じ名前の部品(.py)を作らない。`app/test_mount.py` が clip-studio・cut2resolve・transcribe-tool・app・ytt_core を調べる。実行時にも重なれば取り込まない
- データの置き場所はまだ各ツールのフォルダのまま(リポジトリの外へ移すのは段階4)→ 09-26 移動済み(`docs/data-location.md`)
- 取り込んだツールは入口と一緒に動くので、入口の画面の「停止」「再起動」は押せない(「すべて終了」で一緒に終わる)

## 段階3-2(cut2resolve の取り込み)で決めたこと
- 置き場所は `/cut2resolve/`(3-1 と同じ形。`app/mount.py` の `MOUNTS` に1行足しただけ。入口の起動では既定で取り込む)
- cut2resolve の Handler は、単独ではサーバー(`C2RServer`)の `port`・`allowed_hosts`・`app` を使い、取り込まれたときは `prepare()` が作る `MOUNT`(同じ3つを持つ)を使う(`Handler.ctx`)。
  ジョブ・配信を許す動画の登録などの状態はこれまでどおりサーバーごと(テストで複数のサーバーを立ててもぶつからない)
- CSP は cut2resolve 自身のもの(`script-src 'self'`。YouTube も許さない)をそのまま使う。`MOUNTS` の csp を None にすると、ツールの `CSP` を使う。
  取り込むときに script-src にインラインを許す CSP なら取り込まない(`MountError` → 別のプログラムとして起動)
- 画面: 部品は相対パス、API・動画(`/media/<合言葉>`)の URL は `app.js` の `BASE`(画面の場所)を前に付ける。書き込み系の要求に `X-YTT-Token`
- 終了(`finish()`): 動いている書き出しを取り消し(ffmpeg を止める)、自分の `.runtime` を消す。二重起動の防止はスタジオと同じ(start.bat は入口の中の画面を開く)
- cut2resolve の `.runtime`・siblings はまだ自分の写し(ytt_core は使っていない)。形は ytt_core と同じ(`path`・`paths`)。ytt_core への切り替えは Resolve パックの一本化と一緒に行う
  → 09-26 切り替え済み(`.runtime`・siblings・Host/Origin/Sec-Fetch の検査を ytt_core に。cut2resolve のフォルダだけでは動かず、隣に ytt_core が要る)
- 残るリスク(同じアドレスになったため): スタジオの画面に XSS があれば、合言葉も読めるので cut2resolve の書き出し(指定したフォルダへのファイルの書き込み)まで届く。
  両方の画面の CSP(`script-src 'self'`)がこれを防ぐ前提。画面を変えるときはインラインのスクリプト・`innerHTML` への外の文字列を入れない。
  入力欄の前回の値(localStorage)は、入口の中と単独で別になる(アドレスが違うため)。同じアドレスの他のツールからは読める(パスが入るだけで秘密は入れない)

## 段階3-3(文字起こしの取り込み・認識のワーカー分離)で決めたこと
- 進め方(ユーザー判断 09-25): ワーカー分離と取り込みを一度に行う
- 認識ワーカー `transcribe-tool/tx_worker.py`(別プロセス)。faster-whisper(ctranslate2)・sherpa-onnx・numpy は**ワーカーの中だけ**で読み込む。
  入口のプロセス(スタジオ・cut2resolve も同居)にネイティブのライブラリを入れないので、認識が落ちても落ちるのはワーカーだけ(`test_worker.py` が import されていないことを検査)
- やり取り: ワーカーの標準入力/出力に1行1件の JSON(ASCII)。要求は1度に1つ(ジョブのスレッドは1本のまま)。ネットワークのポートは開けない(攻撃面を増やさない)。
  ワーカーの標準出力はやり取り専用(ライブラリ・ネイティブの出力は fd を付け替えて worker.log へ)
- 変更を小さくするため、サーバー側の `load_model()` は代理の `RemoteModel` を返す(`transcribe()` が faster-whisper と同じ形)。ジョブの流れ(行の整形・保存・再認識・比較)は変えていない。
  音声の範囲はパスとサンプル番号で渡し、サーバー側で音声をメモリに読まない
- 落ちたとき: そのジョブは「失敗」(理由と worker.log の案内)。次の要求で起動し直す。取り消しは既存の `job["proc"]` の仕組みで伝え、15秒で止まらなければ強制終了して「中止」。
  入口・サーバーが落ちてもワーカーは標準入力が閉じたのを見てすぐ終わる(取り残されてメモリを持ち続けない)
- メモリ: 15分(`TRANSCRIBE_MODEL_IDLE_SEC`)使わなければワーカーごと終わらせる(以前はモデルだけ手放していた。プロセスを終わらせる方が確実に OS に返る)。
  GPU の有無も別プロセス(`tx_worker.py --probe`)で1回だけ調べる
- Python: Windows は入口と同じ Python。Mac/Linux で `transcribe-tool/.venv` があればワーカーはそちら(faster-whisper の入れ先)
- 画面: インラインの JS を `app.js`・`ui-kit.js` に出した(CSP `script-src 'self'`)。`apiUrl()` の BASE・合言葉は cut2resolve と同じ形
- テスト用: `TRANSCRIBE_BACKEND=worker-fake`(サーバーは本物の経路、ワーカーの中だけ偽のモデル)・`TRANSCRIBE_WORKER_CRASH=<n>`。クラウドでは本物の faster-whisper を入れられない(PyPI に届かない)ため、
  **本物のモデルでの確認は Windows の実機で必要**
- 残るリスク: 文字起こしの画面も同じアドレスになったので、3つの画面のどれかの XSS で他のツールの API に届く(3つとも CSP `script-src 'self'` と合言葉で防ぐ前提)。
  ワーカーの起動(serve.py の読み込み)に1秒ほどかかるので、最初の文字起こし・15分空いたあとの文字起こしは、モデルの読み込みと合わせて少し遅れる

## 段階4(案件とジョブ管理)で決めたこと
- 作業データの置き場所: `%LOCALAPPDATA%\youtube-tools\<ツールID>\`、全部、コピーで移行(`docs/data-location.md`)。09-26 実機で移行・片付けまで確認済み
- 案件(ユーザー決定 09-26「案件ファイルを持つ」): 案件 = スタジオの動画1本。入口の「案件」の画面(`/cases.html`・`app/cases.py`)。
  紐づけは開くたびに各ツールのデータから組み立て直し、`app\cases.json` には人が付ける状態・メモと「最後に見えた紐づけ」だけを持つ(各ツールの記録と食い違わないため)。
  各ツールのデータは読むだけ
- 紐づけの規則は `ytt_core/txindex.py` の1か所(案件の画面とスタジオのセリフの表示が共通で使う): 文字起こしの sourcePath = 書き出した mp4、無ければ文書の clip(.clip.json)の配信・マーク。
  複数あれば更新が新しいもの。文書は更新日時・大きさが変わったときだけ読み直す
- 同時実行の上限(`ytt_core/jobs.py` の `SLOTS`): スタジオの解析・書き出し、文字起こし、cut2resolve のパック作りを、1つのプロセスの中で合わせて**2つまで**(順番どおり。
  環境変数 `YTT_MAX_HEAVY_JOBS` 1〜8)。順番待ちの間は各ツールの画面に「他のツールの処理が終わるのを待っています」、待っている間も中止できる。
  単独起動(start.bat)のときはそのツールの中だけで数える。当初案(文字起こし 1・ffmpeg 2 のように種類ごと)より単純な「全体で2」にした
  (実機で詰まる・遅いと感じたら `YTT_MAX_HEAVY_JOBS` で調整。種類ごとの上限は、実際に困る場面が出てから)
- 文字起こし → スタジオへ返す: スタジオの ③ 確認で、書き出し済みのマークに「セリフ」(`GET /api/transcripts?id=<動画ID>`・`clip-studio/txlink.py`)。
  時刻は元の配信の時刻(`schemas.clip_offset`: 文書の clip → mp4 の隣の .clip.json → マークの開始の順。最後の場合は画面に注記)。行を押すとその行を再生。
  ネットワーク上のパスの .clip.json には触らない。字幕の文字は必ずエスケープ(同一オリジンのため)

## 段階5・6で決めたこと(09-26)
- まとめて実行(`app/autorun.py`・案件の画面の各配信): 形はユーザーが選ぶ(ユーザー決定「選べるようにする」)。
  採用後を全部(書き出し → 文字起こし → パック)/ 文字起こしまで / 解析から全部(未解析なら解析 → 自動マークの点数の上位 N 件を採用 → …)
  - 各ツールの画面と同じ API を HTTP で順に呼ぶ(同じ検査・ジョブ管理・重い処理の順番待ちを通すため。ツールの関数を直接呼ばない)
  - まだ無いものだけを作る(書き出し済み・文字起こし済み・パック済みは飛ばす)ので、止めてももう一度押せば続きから。同じ名前のパックは上書きしない
  - 自動で採用したマークは学習の記録(スタジオの feedback)に入れない(機械の選択を人の判断として学習すると自己強化になる)。スタジオの `POST /api/video/adopt-top`
  - パックの区間は `pack.TRANSCRIPT_ROWS`(文字起こしの Resolve パッケージと同じ)を cut2resolve の API の `preset: "transcript-rows"` で使う。字幕は校正前(校正したら作り直す)
  - 解析の設定は既定値(解析の画面の設定はブラウザの localStorage にしか無い)→ 段階7-1 でスタジオのサーバーに保存し、まとめて実行も同じ設定を使うように変えた。書き出し・文字起こしは各ツールに保存された設定
  - 実行の記録は入口のメモリの中だけ(入口を終えると消える。まだ無いものだけ作るので、押し直せばよい)
- ラウドネス調整(ユーザー決定「スタジオの書き出し時」): 既定 -14 LUFS(-11・-16・-18・そろえない)。ffmpeg の loudnorm で測って、切り抜きと編集用素材に**同じ量**の音量をかける
  (Resolve で差し替えても音量が変わらない)。ピーク -1 dBTP と +20dB で上げ止め。無音は触らない
- 単独起動の廃止(ユーザー決定): 各ツールの start.bat・start.command を `tools/removals.txt` に載せ、push.bat が `git rm` する(Cowork は PC のファイルを消せないため。以後の削除も同じ仕組み)。
  serve.py の単独で動く部分は、入口に取り込めなかったときの子プロセスとテストのために残す
- README の一本化: リポジトリ直下の `README.txt` が全体の使い方。各ツールの `README.txt` は細かい使い方と変更の記録
- コミット前の検査(`tools/push_helper.py check`): キーの形・個人データの名前・作業データのフォルダ・動画・5MB 超があれば push.bat が止まる
- 保留(ユーザー決定): 新着配信の監視、画面をブラウザのままにするか pywebview にするか(現状維持)

## 段階7(画面の形)で決めたこと(09-26)
ユーザー決定: 7-3 まで進め、窓を数日試してから「窓で十分 / pywebview(7-4)へ / ブラウザに戻す」を決める。7-4 は依存の追加なので、改めて確認してから。
- 7-0 画面のエラーの記録: 入口と3ツールの画面の捕まえられなかったエラーを `app\logs\client-errors.jsonl`(1行1件の JSON)へ。ui-kit(`UIKit.report`)が送り、`app/clientlog.py` が書く。
  1分に30件・512KB で回す・同じエラーは画面ごとに1回。窓でもブラウザでも、落ちたときに AI がログから追えるようにするため
- 画面の共通の API `api/ytt/…`(client-log・open-window・open-external): 画面は相対パスで呼び、入口の画面は `/api/ytt/…`、取り込んだツールは `/studio/api/ytt/…` など。
  `app/mount.py` がツールに渡さず入口(`PortalServer.ytt_request`)へ回す(ツールごとに同じものを書かない)。検査は入口の API と同じ
- 7-1 解析の設定をスタジオのサーバーへ(`/api/settings` の `analyze`。settings() の形・noCache は除く)。以前の localStorage は、サーバーに無いときだけ1回引き継ぐ。
  まとめて実行の「解析から全部」も同じ設定を使う(窓は専用のプロファイルなので、ブラウザの中の保存は引き継がれないため)
- 7-2 離れた・戻った(`UIKit.life`): タブの切り替え・別の窓へ移った(blur)・閉じる直前(pagehide)。埋め込み(iframe)にフォーカスがあるときは離れたことにしない。
  `'blur'` のときは保存だけ(再生の停止・文字起こしの保管(音声の切り出し)はしない)。画面は visibilitychange を直接使わない
- 7-3 窓で開く(試用。`app/appwindow.py`): Edge の `--app=<URL> --user-data-dir=<作業データの app\browser-profile>`。設定は `app\settings.json` の `"window"`、既定はブラウザ。
  窓の中の「新しいタブで開く」は ui-kit(`UIKit.win`)が入口に頼む: このパソコンの画面 → 窓、外のサイト → いつものブラウザ。窓かどうかは `display-mode: standalone`
  - 安全: 窓で開けるのは入口と動いているツールのポートの画面だけ、外は http/https だけ、10 秒に 8 回まで、シェルを通さない、URL の空白・引用符を断る(Edge の引数に化けない)。
    リモートデバッグのポートは開かない。窓を閉じても入口は終わらない(閉じる直前の保存よりサーバーの停止が先になる危険を持ち込まない)
  - 実機で確かめること(クラウドの Linux では Edge の窓を再現できない): 窓の中のリンクの行き先・YouTube の埋め込み(専用のプロファイルは未ログイン。会員限定などは再生できないことがある)・書き出し中に落ちないか

## 主なリスクと対策
優先度の高いものと、AI が作業するときの守り方は `AGENTS.md` の「統合作業のリスク」にもある(実装する AI はそちらを必ず読む)。
- 【高】複数の AI の同時編集: ツール単位の担当表(担当中は他の AI が触らない)、ファイル移動は `git mv` を1コミットにまとめ前後で WORKLOG に告知、長く分かれたブランチは使わない(同じフォルダを2つの AI が使うため)
- 【高】同一オリジン化(段階3)で、1つの画面の XSS が全ツールの API(ファイルの書き込み・Resolve へのスクリプト登録)に届く: CSP `script-src 'self'` の維持、インラインスクリプトの外部化、書き込み系 POST に CSRF トークン、Host/Origin 検査の共通化、パスは許可フォルダだけ
- 【高】1プロセス化での障害の波及: 文字起こし(faster-whisper)は別プロセスのワーカーに分け、落ちたら再起動。同じプロセスに読み込まない
- 【高】Resolve パックの二重実装(`resolve_export.py` と `pack.py`)の一本化: 先に契約テストを書き、それが緑のまま寄せる → 09-26 済み。以後はパックを作るのは `pack.py` だけ(契約テストが二重実装への逆戻りを防ぐ)
- 【高】文書の食い違い: `claude/*.md`(Project)と `docs/project/` は写しで古いことがある。正本は claude.ai の Claude Docs
- CPU の取り合い: ジョブ管理で同時実行数を制限 → 09-26 実装(重い処理は全体で2つまで。`ytt_core/jobs.py`)
- データ移行: コピーのみ、元のフォルダは一定期間残す、削除はユーザー確認後
- Public リポジトリへの個人データ混入: 作業データはリポジトリの外に置き(09-26 実装)、コミット前の検査を足す(09-26 実装。push.bat)

## 未決
- 統合後のポート番号(入口 8700。旧起動方法は 09-26 に廃止済み)
- 画面はブラウザのままか pywebview か(09-26: 段階7-3 で Edge のアプリモードの窓を試用に。数日使ってから、窓で十分 / pywebview(7-4)/ ブラウザに戻す を決める)
- 新着配信の監視(09-26 ユーザー判断: 保留)
