# HANDOVER — 次のセッションへの引き継ぎ(2026-09-26)

セッションを切り替えるたびに上書きする。詳しい経緯は `docs/WORKLOG.md`、ルールは `AGENTS.md`。

## 今の状態
- GitHub の main = 6b8304d 以降。この後の未コミット(段階4 の残り: 同時実行の上限・案件ファイル・セリフの表示)は WORKLOG の最後の記録を参照(push.bat で送る)
- 版: 入口 0.6.0・切り抜きスタジオ 0.5.0・文字起こし 0.14.0・cut2resolve 0.8.0・ytt_core(jobs・txindex を追加)
- 統合計画: 段階3 完了。段階4 は一通り実装済み(置き場所の移動は実機確認済み。案件ファイル・同時実行の上限・セリフの表示は実機確認待ち)。段階5 は未着手
- 置き場所と案件ファイル: `docs/data-location.md`。段階4 の決めごと: `docs/integration-plan.md` の「段階4で決めたこと」
- 以前の場所の片付け・`0old`・`.whisper_models` の削除はユーザーが済ませた(09-26)
- 資料の正本はリポジトリ。統合計画だけは Claude Docs「動画編集ツール 統合計画」が正本(09-26 の段階4 まで更新済み)
- ユーザーの指示: **作業中は 10〜20 分ごとに中間報告する**

## 次の作業(上から順に)
1. 実機確認(ユーザー): push.bat → 「すべて終了」→ start-all.bat。入口の上の「案件(配信ごと)の一覧 →」で配信・切り抜き・文字起こし・パックが並ぶか、
   状態・メモが保存されるか。スタジオの ③ で書き出し済みのマークに「セリフ」が出て、行を押すとその時刻が再生されるか。重い処理を3つ以上始めて順番待ちの表示が出るか
2. 段階5(一括実行・新着配信の監視・ラウドネス調整)の進め方をユーザーと決める

## 注意
- Cowork(クラウド)は PC で git・移動・削除ができない。書き込んだら読み直して一致を確かめる。push はユーザーの push.bat
- **サーバーを動かすテストは先頭で `os.environ.setdefault("YTT_DATA_DIR", "inplace")`**(忘れると本物の作業データを読み書きする。ytt_core のテストが検査)
- 重い処理(ffmpeg・認識など)を足すときは `ytt_core.jobs.SLOTS.slot(...)` を通す。文字起こしと切り抜きの紐づけは `ytt_core/txindex.py` だけ。他のツールのデータは読むだけ
- 契約テスト `tools/test_resolve_pack_contract.py` は単独のコマンドで(app/test_mount.py と一緒に渡すと部品の名前が重なる)
- GPT(Codex)がこのフォルダで作業すると `.git\refs\codex\` に壊れた参照が残ることがある → push.bat が `bad object refs/codex/…` で止まったら `rmdir /s /q .git\refs\codex`
- 起動中の入口・ツールは古いコードのまま。コードを直したら「すべて終了」→ start-all.bat
- 文字起こし側に Resolve 用の計算を書き足さない(二重実装に戻さない。契約テストが検出する)
