==========================================================
  切り抜きスタジオ  v0.5.0  はじめに読んでください
==========================================================

【これは何?】
  「配信ランキング」「自動切り抜き」「切り抜きマーカー」の3つを1つにまとめたツールです。
    ① 探す      ホロライブ・にじさんじ・ぶいすぽっ!・ネオポルテの人気配信を一覧にし、解析したい配信にチェック
    ② 解析      チェックした配信を順番に自動解析(音量・チャット・コメント欄)。終わったものから確認できます
    ③ 確認      自動で付いたマークをプレイヤーで見ながら直し、必要なものだけ mp4 に書き出します
    ④ コラボ    複数人のコラボ配信をグループにまとめ、一人が採用したマークを他の人の配信にも候補として転写します
  ※ 合成動画・チャット・疑似のYouTube応答でテストしています。Windowsではローカル動画の書き出し・音量調整、
    ブラウザ上での保存失敗と復旧も確認しています。実際のYouTube(取得・埋め込み・ライブ配信)は未検証です。

【v0.5.0 の変更(2026-09-26・セリフの表示・重い処理の同時実行の上限)】
  ・③ 確認で、書き出し済みのマークの下に「セリフ」を出します。文字起こしツールでその切り抜きを文字にすると現れます
    (行数と校正の進み具合つき。行を押すとその行だけ再生。文字起こしで「カット」にした行は線を引いて表示)。
    時刻は元の配信の時刻に直してあります(切り抜きの隣の .clip.json の情報を使います。無いときはマークの開始に合わせ、
    高速書き出しの切り抜きは数秒ずれることがあります)。文字起こしのデータは読むだけで、書き換えません。
    文字起こしのタブで直してからスタジオのタブに戻ると、読み直して最新にします。
  ・解析と書き出しは、他のツールの重い処理(文字起こし・Resolve 用のパック作り)と合わせて同時に 2 つまでにしました
    (入口の中で動くとき。CPU の取り合いで全部が遅くなるのを防ぐため)。順番待ちの間は「他のツールの処理が終わるのを待っています」と出て、
    中止もできます。上限は環境変数 YTT_MAX_HEAVY_JOBS(1〜8)で変えられます。

【v0.4.0 の変更(2026-09-26・作業データをリポジトリの外へ)】
  ・データ(data.json・API キーの config.json・設定・cache・archive・ログ)の置き場所を %LOCALAPPDATA%\youtube-tools\studio に変えました
    (例 C:\Users\<名前>\AppData\Local\youtube-tools\studio。OneDrive で同期されない場所)。
  ・最初の起動で、このフォルダにある以前のデータを新しい場所へコピーします(チャットのキャッシュがあると数十秒かかることがあります)。
    元のデータは消しません。新しい場所で問題なく使えることを確かめてから、このフォルダの data.json・cache などを消してください。
    空き容量が足りない・コピーに失敗したときは移さず、以前の場所のまま動きます(黒い画面に理由が出ます)。
  ・書き出し先: 設定で決めていればそのまま。以前の既定(このフォルダの exports)を使っていた場合は、そこを使い続けます。
  ・置き場所は入口の画面(http://localhost:8700/)の「作業データの置き場所」に出ます。

【v0.3.0 の変更(2026-09-25・入口への取り込み)】
  ・入口(youtube-test フォルダの start-all.bat)から起動すると、スタジオは入口と同じアドレスの http://localhost:8700/studio/ で開きます
    (入口のプログラムの中で動きます。黒い画面は入口の1つだけ)。今までどおり、このフォルダの start.bat だけで起動することもできます(http://localhost:8800/)
  ・入口の中で動いている間に start.bat を押すと、2つ目は起動せず、入口の中のスタジオをブラウザで開きます(同じデータを2つのサーバーで取り合わないため)
  ・入口の中で動くときは、安全のため画面に「CSP」(外から入れられたスクリプトを動かさない仕組み)と、書き込みの操作に合言葉を付けます
  ・画面の部品(core.js など)は相対パスで読むようにしました。他のツールの「他のツール」メニューは、入口の中のスタジオへ正しくつなぎます

【v0.2.0 の変更(サーバー側・2026-09-24 の全ツール見直し)】
  ・書き出した動画ごとに、隣へ「<動画の名前>.clip.json」を作ります(例: 01_00h12m34s-00h13m19s.mp4 → 01_00h12m34s-00h13m19s.clip.json)。
    元の配信のどこを切り抜いたか(元の配信の秒・動画ID/元のファイル・マーク)が入っていて、文字起こしツールが自動で読みます。
    前後10秒の編集用素材(*_edit.mp4)にも同じものが付きます。消しても書き出した動画はそのまま使えます。
    速度優先で書き出したときは、実際の開始がキーフレーム(映像の区切り)の分だけ前にずれるので、分かる場合はその秒も入れます(ffprobe が必要。ffmpeg に同梱)。
  ・起動中は、このフォルダの1つ上の .runtime フォルダに studio.json(使っているポート)を置き、終了時に消します。
    他のツールの「他のツール」メニューが、ポートが変わっていても正しいアドレスを開けるようにするためです。
  ・起動時に ffmpeg / yt-dlp の版や書き出し先の空き容量を調べ、yt-dlp が古い(60日以上前の版)・空きが2GB未満のときに、画面で注意を出せるようにしました(/api/state の env)。
  ・修正: 動画の終わりをまたぐマーク(配信の最後など)を書き出すと、音量調整の段階で「空か短すぎます」と失敗することがあった問題
  ・修正: 判定の記録(feedback.jsonl)が大きくなったとき、古い記録が消えることがあった問題(今は feedback.jsonl.old の末尾へ移します。消しません)
  ・修正: registry.json(事務所の登録)が壊れていると、次の保存で登録が初期状態に上書きされていた問題(壊れたファイルは registry.json.corrupt-日時.bak に残します)
  ・修正: Windows で、ほかのアプリが使っているポートでも起動できてしまい、どちらに繋がるか分からなくなることがあった問題
  ・修正: 出力先のパスが長い・動画名が Windows の予約名(COM¹ など)・前回の編集用素材だけが残っている、ときの書き出しの失敗や上書き
  ・ログの控えの名前を studio.old.log / studio-errors.old.log に変えました(以前の studio.log.old は GitHub に載ってしまうおそれがあったため。起動時に自動で改名します)

【v0.1.8 の修正】
  ・保存できなかった編集を保持し、動画切り替え・書き出し・再解析を止めます。再保存後に続けられます。
    未保存の編集や一括書き出しがある間は、ページを閉じる前にブラウザが確認します。
  ・一括書き出し中の通信断を「完了」と扱わず、そのジョブの状態確認を続けます。
    残りの動画は自動開始しません。進捗を確認してから再実行してください。
  ・「全動画の採用を書き出す」は、1動画に51件以上あっても50件ずつ順に処理します。
  ・Windowsの一時的なファイル使用中エラーでは、最大0.7秒待って保存・音量調整後の置き換えを再試行します。
    解消しない場合は対象ファイルと対処を表示し、studio-errors.log に詳細を記録します。
  ・動画作成後に「書き出し済み」の記録だけが保存できなかった場合は、作成済みの動画を保持して注意を表示します。
  ・Windows予約名を含む動画名や、保存先変更後に過去ジョブの出力先表示が変わる問題を修正しました。

【v0.2.0 の変更(画面の刷新・ダーク/ライト切り替え)】
  見た目
  ・3つのツールで共通の見た目(ui-kit)に作り直しました。右上のボタンでダーク/ライトを切り替えられます(初回はパソコンの設定に合わせ、選んだものを覚えます)。
  ・右上に「キー」(キー操作の一覧。? キーでも開きます)、「設定」(右から出る引き出し。③ の作業中でも画面を移らずに開けます)、「他のツール」
    (文字起こしツール・cut2resolve を開く。実際のポートはサーバーに問い合わせ、分からなければ既定のポート)を置きました。
  ・② 解析は「入れる・設定」と「キュー」を左右に並べ、③ は右の列(書き出し・マーク一覧)を画面の高さに収めて一覧だけがスクロールするようにしました。
  ・ダーク表示で ③ のマークの「開始・終了・ラベル」の欄が白くなっていたのを直しました。
  追加した機能
  ・③ で書き出した切り抜きに「文字起こしで開く」「Resolve 用に渡す」(それぞれのツールを、その mp4 を入れた状態で開きます。自動では始めません)と「パスをコピー」。
  ・他のツールのリンク(http://localhost:8800/?url=YouTubeのURL)で開くと、② の URL 欄に入ります(「解析に追加」を押すまで始めません)。
  ・書き出しの全体の進み具合のバー、読み込み中・空のときの表示、操作の結果の通知の色分け(成功・失敗)。
  直した不具合
  ・設定の API キーの欄が空のまま「保存」を押すと、保存済みのキーが消えていた。
  ・事務所の登録で「公式チャンネル」欄に入力中、自動保存のたびにカーソルと「チャンネルを追加」欄の書きかけが消えていた。
  ・③ の前/次のマーク(] [)が、点数順で表示していても時刻順に移動していた(いまは一覧の並びどおり)。
  ・「もう一度押すと…」の確認が、実行後も3秒間残っていて、続けて押すと同じ操作(再解析の依頼など)がもう一度走っていた。
  ・④ の「グループから外す」が1回押すだけで実行されていた(基準の動画を外すとグループごと消えるため、2回押しの確認にしました)。
  ・設定やキー一覧を開いている間も ③ のキー操作が裏の動画に効いていた。ほか、二重送信・通信の順番の入れ替わりの対策。

【準備】
  1. Python 3(https://www.python.org/downloads/ 。Windows は「Add python.exe to PATH」にチェック)
  2. ffmpeg     Windows: winget install Gyan.FFmpeg   / Mac: brew install ffmpeg
  3. yt-dlp     Windows: winget install yt-dlp.yt-dlp / Mac: brew install yt-dlp
     (入れたら黒い画面を開き直してください。旧ツールで入れたものと共通です)
  4. start.bat をダブルクリック(Mac は start.command。初回だけ chmod +x start.command)
     ブラウザで http://localhost:8800/ が開きます。※ 127.0.0.1 ではなく localhost で開いてください(YouTube埋め込みのため)
  5. 画面右上の「設定」(歯車)を開き、YouTube Data API のキーを入れる(ランキングの検索に必要。無くても②③は使えます)

【最初にやること(初回だけ)】
  ・設定 →「事務所の登録」で、4事務所のチャンネルを取り込みます。
    ホロライブ・にじさんじ・ぶいすぽっ! は「公式から取り込み」→「追加して解決」。
    ネオポルテは公式の一覧がないので、チャンネルのURL(または @ハンドル)を入れて「追加して解決」します。
  ・旧ツールの data.json や設定は引き継ぎません(新しく始まります)。

【使い方】
  ① 探す   期間・ワードを決めて検索 → 解析したい配信にチェック(最大10本)→「選んだ配信を解析に追加」
  ② 解析   順番に解析します。進み具合が見えます。失敗した配信は飛ばして続きます(「やり直し」で入れ直せます)。
           1本終わるごとに「確認する」が押せます(他の配信の解析は裏で続きます)。チャット取得が長いときは「チャットを待たずに進める」
  ③ 確認   自動マークが「候補」として入った状態で開きます。1件ずつ 採用 / 不採用 / 候補 を決めて、「採用」だけを書き出します。グラフに音量・チャットの山と、マークの点数・理由(「音量が急上昇」など)が出ます。
           ・マーク①〜⑤のボタン(長さ違いの区間)、I / O(IN・OUT)、±0.5 / 1 / 5秒の微調整、シアター、ライブ配信対応は切り抜きマーカーと同じです
           ・キー配置は「操作の設定」で変えられます(標準 / 左手だけ)。いまの割り当ては右上の「キー」か ? キーで一覧できます
           ・判定: 各マークの「採用」「不採用」「候補」ボタン(書き出すと「書き出し済み」)。上の 全部/候補/採用/不採用/書き出し済み で絞り込み、時刻順/点数順で並べ替えられます
           ・キー(標準): ] 次のマーク / [ 前のマーク(移動するとその区間を自動再生)/ y 採用 / u 不採用(押すと次の候補へ進みます)。「操作の設定」で変更・自動再生や自動で次へ進む動作のオン/オフができます
           ・マークの行は ▾ で折りたたみ(「すべて折りたたむ」「すべて開く」もあります)。「候補をすべて採用」もあります
           ・書き出す対象: 採用のみ(標準)/ 採用+候補 / 不採用以外すべて。「全動画の採用を書き出す」で、採用マークがある全部の動画を順番に書き出します(この画面を開いたままにしてください)
           ・書き出しは切り抜きマーカーの方式: YouTube は必要な範囲だけ取得。精度優先/速度優先、画質の上限を選べます
           ・書き出し先: 標準では %LOCALAPPDATA%\youtube-tools\studio\exports\<動画名>\(v0.4.0 から。以前このフォルダの exports を使っていた場合はそのまま)。変更は設定の「出力先フォルダ」
           ・書き出し済みマークの時刻を動かす、または「採用」を押し直すと、「書き出し済み」が外れます(採用に戻り、書き出し直せます)
  入口は4つあります: ①ランキングから / ②URLを直接貼る / ③手元のファイル(フルパス)/ ④解析せずに確認画面だけ開く(手動でマークだけ付けたいとき)
  ④ コラボ   複数人のコラボ配信で、一人が付けたマークを他の人の配信の「同じ瞬間」にも候補として転写します(自動採用はしません。必ず候補として入り、採用/不採用は人が決めます)。
           1. ② の「解析する配信を入れる」で、まとめて入れた配信を「コラボとしてまとめる」にチェックすると、追加した動画が1つのグループになります(あとから ④ の画面でも、チェックボックスで選んで「まとめる」できます)。
           2. ④ の画面で、グループの各動画(基準の動画以外)に「アンカーを指定」→ 両方の動画で同じ瞬間を見つけて時刻を入力(1点だけでもよい。2点指定すると配信中のズレの変化(ドリフト)にも対応します)。
           3. これでズレの対応ができたので、以降はどちらかの配信でマークを「採用」にするたびに、同じグループの他の配信にも、その瞬間(前後2〜3秒広げた範囲)が「候補」として自動で追加されます。採用するかどうかは、その配信を見ながら人が決めてください。
           ・グループから動画を外しても、すでに転写された候補・採用済みマークは残ります。基準の動画を外す、または残り1本になる場合はグループごと削除されます(ズレの基準が失われるため)。

【解析のしかた】(自動切り抜き v0.2.0 と同じ)
  音声・チャット・コメント欄を、それぞれ「ふだん(前後5分の中央値)からどれだけ跳ね上がったか」で点数にして足し合わせます(重みは 音声1.0・チャット1.0・コメント0.7)。
  v0.1.4 の変更:
  ・チャットの遅れを動画ごとに自動推定します(音量の山とチャットの山の一致から。詳しい設定の「チャットの遅れを自動で推定する」。はっきりしないときは従来の「遅れ補正」の秒数を使います)。推定した秒数は解析結果の注意書きに出ます。
  ・配信の冒頭は、0秒で0倍 → 「冒頭の減点」の秒数(標準180秒)で1倍へ、なだらかに点数を戻します(挨拶・BGMの誤検出対策。0で無効)。
  ・コメント欄の時刻が1つのコメントに3つ以上ある場合(チャプター一覧)は、1つあたりの重みを 1/個数 に下げます。
  ・チャットの「！」1つだけでは盛り上がりに数えません(「！！」以上・「草」・「w」などは数えます)。
  v0.1.5 の変更:
  ・後から精度を見直すためのデータを記録するようにしました(解析の結果や画面の動きは変わりません)。
      - archive フォルダ: 解析した配信ごとに、1秒ごとの音量・チャット(草の件数・発言した人数・スパチャ)・コメント欄の時刻と文面の先頭60文字・解析ごとの設定と候補(直近5回分)を圧縮して保存します(2時間の配信で約40KB。最大2000本)。
      - cache/meta: YouTube の動画情報(みんなが繰り返し見た場面・チャプター・カテゴリ・タグ・再生数・登録者数)。yt-dlp で取得します(取れない動画・配信直後は記録なし。取れなくても解析はそのまま進みます)。
      - feedback.jsonl: 手動で切った区間を採用・不採用にしたとき、書き出したときも記録します。自動の候補には、最初の区間(auto0)と手で直した量(dStart / dEnd)、操作(adopt / reject / delete / export)も付きます。
  ・詳しい設定に「配信タイプ別の重み(試験的)」を追加(初期はオフ)。タイトル・タグ・カテゴリから、ゲーム/雑談/歌枠/その他を推定して重みの倍率をかけます(歌枠は音声×0.6、雑談は音声×0.8・チャット×1.1、いずれも仮の値)。タイプは手で指定もできます。オフでも、推定したタイプは記録します。
  ・改善のためにデータを送るときは、data.json・feedback.jsonl(feedback.jsonl.old があればそれも)・archive フォルダをお渡しください。
  区間は点数の山を65%あたりに置き、近くの静かな所に端を寄せます。感度・本数・長さ・重みは②の設定で変えられます。
  チャットの取得は配信の長さ(チャット数)に比例して時間がかかります。複数本を解析するときは、今の配信の解析中に「次の配信のチャット」を裏で先に取っておきます(v0.1.3〜、同時に1本まで)。
  自分の番が来たときは待たずに使えるので、まとめて解析するほど全体が短くなります(1本だけのときは変わりません)。
  同じYouTube動画の再解析は、音量の解析結果とチャットをキャッシュ(cache/)から使うので速く終わります。配信中・直後の動画は「キャッシュを使わない」をオンに。

【フィードバック(自動で記録)】
  自動マークの「採用」・書き出し=「よかった」、「不採用」・候補のまま削除=「ちがった」として、feedback.jsonl に1行ずつ残ります(画面での集計はまだありません。感度・重みの見直し用)。

【旧ツールから消した機能】
  ・切り抜きマーカー: コメント欄からの候補抽出、CSV/TSV の出力・読み込み
  ・自動切り抜き: 見つけたらそのまま書き出す(自動書き出し)、○×ボタン(採用/不採用に置き換え)
  ・文字起こしツールは含みません(保留中)
  旧ツールのフォルダはそのまま使えます(このツールとは別のデータです)。

【データについて】(すべてこのフォルダ内)
  data.json(動画・マーク。壊れたときは data.json.corrupt-日時 に退避して新しく始まります。.bak は直前の控え)/ settings-ui.json / registry.json / config.json(APIキー)/
  feedback.jsonl / cache/(チャット・音量・グラフ)/ work/(作業用・自動で消えます)/ exports/(書き出し結果。動画ごとに .clip.json も)/
  studio.log・studio-errors.log(1MB を超えると *.old.log に1世代だけ残します)
  他の人にフォルダを渡すときは config.json を抜いてください。解析結果のグラフは cache/series/ に保存され(最新60本)、再起動後も表示されます。

【できないこと・注意】
  ・笑い声や叫び声を聞き分けているわけではありません。大きい音・高い音の急上昇を見ています。ゲームの効果音・BGM・音量の急変にも反応します
  ・チャットのリプレイは、無効・削除された動画、メンバー限定チャットでは取れません(音声だけで判定します)
  ・メンバー限定・年齢制限・非公開の配信は取得できないことがあります
  ・縦動画(9:16)への変換・字幕の焼き込みはありません(横のまま書き出します)
  ・コラボのズレは今のところ手で指定するだけです(音声どうしの自動推定はまだ)。ズレが一定でない配信(回線落ち・広告の位置が動画ごとに違うなど)には、まだ十分に対応できません
  ・グループの基準の動画を外す(または動画ごと削除する)と、グループごと削除されます。すでに転写されたマークは残りますが、グループを作り直してズレを指定し直す必要があります
  ・YouTube から取得した映像には、著作権・利用規約が関わります。自分の配信や、許諾を得たものにだけ使ってください
  ・サーバーは自分のPC(127.0.0.1)だけで待ち受けます。外に公開しないでください

【困ったとき】
  ・途中で終了して黒い画面も閉じてしまう: v0.1.3 から、終了時に画面を閉じず理由を表示し、studio.log(このフォルダ)に終了の原因を記録します。
    起きたら、黒い画面のメッセージと studio.log(と studio.crash.log があればそれ)を教えてください
  ・画面の上に赤い帯: 古いサーバーが残っています。黒い画面を閉じて、start.bat を起動し直してください
  ・YouTubeのプレイヤーが出ない / 黒いまま: 必ず http://localhost:ポート/ で開いてください。広告ブロッカーが埋め込みを止めていないかも確認を
  ・「音声を取得できませんでした」: yt-dlp が古い可能性があります。yt-dlp -U で更新してください
  ・ポート8800が使用中のときは次の番号(8801…)で起動します。黒い画面に表示されたアドレスを開いてください

DaVinci Resolve用の編集余白
---------------------------
書き出し時に、通常の切り抜きとは別に前後10秒を含む編集用素材（*_edit.*）と
対応情報（*.edit.json）を同じフォルダへ作ります。通常の切り抜きの長さは変わりません。
文字起こしツールのResolveパッケージはこの素材を自動で使うため、Resolve上でカット位置を
前後へ動かしたり、削除区間をSOURCE_WITH_HANDLESタイムラインから復活できます。
